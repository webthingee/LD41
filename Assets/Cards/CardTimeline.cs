using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Playables;
using UnityEngine.Animations;

public class CardTimeline : MonoBehaviour 
{
    PlayableDirector pd;

	// private void Awake()
    // {
    //     pd = GetComponent<PlayableDirector>();
    //     var binding = pd.GetGenericBinding(this);
    //     var outputs = pd.playableAsset.outputs;
    //     var inputOne = pd.playableGraph.GetOutput(0);

    //     pd.SetGenericBinding(PlayableBinding, this.gameObject);
    // }
	
	// // Update is called once per frame
	// void Update () {
		
	// }
    
}
